import React from 'react'

//using ([folder name]) does not adds to the route path. good for organizing our routes.
const LoginPage = () => {
  return (
    <div>LoginPage without /auth/login</div>
  )
}

export default LoginPage